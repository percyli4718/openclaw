# 保客通 (BaokeTong) - AI+ 保险获客系统

# Hermes Agent 核心依赖
nous-hermes-agent>=0.1.0

# AI 模型
# SaaS 模式：使用云端 Claude API
anthropic>=0.18.0
# 私有化模式：使用本地 Ollama
# ollama>=0.1.0  # 可选，私有化部署时启用

# Web 框架
fastapi>=0.109.0
uvicorn>=0.27.0

# 数据库
# PostgreSQL 驱动
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
# Redis 缓存
redis>=5.0.0
# 向量数据库 Qdrant
qdrant-client>=1.7.0

# 数据处理
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
httpx>=0.26.0
aiohttp>=3.9.0

# 向量嵌入
sentence-transformers>=2.3.0

# JWT 认证
python-jose[cryptography]>=3.3.0

# 监控与日志
langfuse>=2.20.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
